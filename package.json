{
  "name": "node-test-patterns",
  "type": "module",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start:dev": "node --watch --no-warnings src/server.js",
    "pg-docker": "docker run --rm -e POSTGRES_PASSWORD=pg_docker -p 5432:5432 -d postgres",
    "test": "vitest"
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3",
    "nodemailer": "^6.9.1",
    "pg": "^8.10.0"
  },
  "devDependencies": {
    "vitest": "^0.29.2"
  }
}